namespace Domain.Base
{
    /// <summary>
    /// Feature interface for containers that can restrain otherwise active critters inside
    /// from leaving the container on their own.
    /// </summary>
    public interface IPreventsThingsInsideFromGettingOut
    {
    }
}